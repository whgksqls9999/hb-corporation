{
  "name": "hb-corporation-dev",
  "private": true,
  "description": "Dev/test scaffolding for the hb-corporation plugin. NOT shipped — the plugin lives in ./plugin (marketplace source).",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
